// Paste into ~/.config/niri/config.kdl

// Mod+M — summon the master control terminal
binds {
    Mod+M hotkey-overlay-title="FENNIA Master Control" {
        spawn "ghostty" "--config-file=/home/YOURUSER/.config/ghostty/mct" "--class=fennia.mct" "--title=FENNIA MCT" "-e" "/home/YOURUSER/.local/bin/mct";
    }
}

// Give it its own window rule: opens maximized in the column, orange ring,
// never gets a shadow-less floating state by accident.
window-rule {
    match app-id="^fennia\.mct$"
    open-maximized true
    draw-border-with-background false   // else the ring paints a solid rectangle behind ghostty
    focus-ring {
        active-color "#ff8a00"
    }
    border {
        active-color "#ff8a00"
        inactive-color "#4d443b"
    }
}
